#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n1,n2;
    scanf("%d%d",&n1,&n2);
    if(n1!=n2)
        printf("Not Same");
    else{
        int arr1[n1],arr2[n2],s1=0,s2=0;
        for(int i=0;i<n1;i++){
            scanf("%d",&arr1[i]);
            s1+=arr1[i];
        }
        for(int i=0;i<n2;i++){
            scanf("%d",&arr2[i]);
            s2+=arr2[i];
        }
        printf("%s",(s2==s1)?"Same":"Not Same");
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
